{
  "name": "@wakiewakie/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "db:generate": "drizzle-kit generate:sqlite --out ./src/database/migrations --schema src/database/schemas",
    "db:migrate": "tsx src/database/migrate.ts"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "drizzle-kit": "^0.20.6",
    "eslint-plugin-drizzle": "^0.2.2",
    "tsx": "^3.12.2"
  },
  "dependencies": {
    "@hono/node-server": "^1.3.3",
    "better-sqlite3": "^9.2.2",
    "drizzle-orm": "^0.29.1",
    "hono": "^3.11.7"
  }
}
