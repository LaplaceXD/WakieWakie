{
  "name": "@wakiewakie/server",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "dev": "tsx watch src/index.ts",
    "db:generate": "drizzle-kit generate:sqlite --out src/database/migrations --schema src/database/models",
    "db:migrate": "tsx src/database/migrate.ts"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/cors": "^2.8.17",
    "drizzle-kit": "^0.20.6",
    "eslint-plugin-drizzle": "^0.2.2",
    "tsc-alias": "^1.8.8",
    "tsx": "^3.12.2"
  },
  "dependencies": {
    "@apollo/server": "^4.9.5",
    "better-sqlite3": "^9.2.2",
    "cors": "^2.8.5",
    "drizzle-orm": "^0.29.1",
    "express": "^4.18.2",
    "graphql": "^16.8.1",
    "graphql-tag": "^2.12.6",
    "zod": "^3.22.4"
  }
}
